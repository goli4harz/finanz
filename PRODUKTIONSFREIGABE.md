# Produktionsfreigabe

Stand: 2026-08-02. Ampel-Bewertung je Bereich nach Abschluss der 23 kritischen Funde (Priorität 1) UND 14 von 15 eindeutigen "hoch"-Funden (Priorität 2) des Härtungsauftrags. Nur noch A2 (Architekturumbau auf Stored Procedures, bewusst zurückgestellt) sowie die 21 "mittel"/6 "niedrig" eingestuften Funde aus `FEHLERANALYSE.md` sind offen.

**Legende:** 🟢 grün = keine bekannten kritischen/hohen Lücken, 🟡 gelb = kritische/hohe Lücken behoben, aber ohne echten Live-Lauf verifiziert oder mit bekannten Restlücken (mittel/niedrig), 🔴 rot = bekannte ungelöste kritische Lücke.

| Bereich | Ampel | Begründung |
|---|---|---|
| Datenmodell (SQL-Schema) | 🟢 | Vollständiger Objektabgleich (G3): 0 von 35 referenzierten DB-Objekten fehlen. `sql/038`+`sql/039` **vom Nutzer erfolgreich ausgeführt** (2026-08-01T21:24:02Z, Execution 24086, `{"success": true}`). `sql/040` (reine Kommentar-Korrektur, keine Schema-Änderung) steht noch zur Ausführung bereit (Workflow `97`). |
| Kursdaten (02/02b) | 🟢 | C1 (period=3mo→1y in `02`) behoben, live gegen die echte FastAPI getestet (AAPL 251 Tage, SAP.DE 253 Tage). C6 (Datenqualitätsstatus-Kollaps beim Schreiben nach `stock_price_history`) behoben - macht den bereits deployten C9-Fix in `14` jetzt tatsächlich scharf. C7/C8 (Sitzungsstatus-Erkennung in `02b`) auf 5 Zustände vervollständigt, beeinflusst jetzt `combined_regime` (→`vorlaeufig` bei laufender Sitzung). Kein echter End-to-End-Lauf von `02`/`02b` seit den Fixes beobachtet (nur die FastAPI-Anbindung selbst live getestet, nicht der volle Workflow). |
| News-Pipeline (03/03a/04/08) | 🟢 | D3/D4/D5/D13 (kritisch) weiterhin behoben und live aktiv. D1/D2/D6 (dynamische Watchlist im KI-Prompt, Ticker-Validierung, Konfidenzfelder), D9 (URL-Kanonisierung + `content_hash`) und D11/D12 (Datum- statt Index-Abgleich, echte Handelszeiten je Börse) jetzt ebenfalls behoben, live gepusht, lokal umfangreich getestet (teils mit direktem Alt-/Neu-Codevergleich, der den jeweiligen Fund reproduziert). Nur noch D7/D8/D10 (mittel) offen. |
| Empfehlungen (06) | 🟢 | A7/A8 (Wertebereichs-/Zielobjekt-Regelwerk für Lernvorschlagsziele) jetzt vollständig über die zentrale `RULE_TABLE` in `12` abgedeckt (alle 5 Vorschlagstypen). `06`s eigener Regime-Matrix-Fallback (`|unknown`) verträgt den neuen `combined_regime='vorlaeufig'`-Wert aus C8 bereits robust, keine Änderung an `06` selbst nötig. |
| Portfoliorisiko/Paper-Trading (14) | 🟡 | Alle 9 kritischen Punkte (E1-E12, C9) weiterhin behoben. **`sql/039` ist jetzt ausgeführt** - die zusätzlichen Spalten/der Unique-Index existieren live. C9s Sitzungsstatus-Check ist seit C6 tatsächlich scharf (nicht mehr nur vorbereitet). Workflow bleibt weiterhin bewusst **inaktiv**, kein echter Lauf gegen die reale Datenbank beobachtet. **Vor Aktivierung: mindestens einen manuellen Testlauf beobachten.** |
| Lernagent News (09) | 🟢 | F1/F2 (kritisch) unverändert behoben, live aktiv (nächster Lauf Samstag). F3 (mittel) offen. |
| Lernagent Strategien (09b) | 🟡 | F4/F5 unverändert behoben. Workflow bleibt bewusst **inaktiv** - OOS-Gate strukturell erst nach dem Backtesting-Modul (AP7, dormant) sinnvoll nutzbar. |
| Orchestrator (00) | 🟡 | B1/B4/B5 (kritisch/hoch) weiterhin behoben. **B2 (hoch, Teilausfall trotz Gesamterfolg) jetzt ebenfalls behoben** - drei neue Nodes machen einen `partial_failure`/`skipped` einer Stufe sichtbar (Matrix-Meldung + `metadata_json`) statt ihn stillschweigend wie vollen Erfolg zu behandeln. Lokal mit 4 Szenarien getestet, **kein echter Orchestrator-Lauf seit den Fixes beobachtet** (nächster planmäßiger Lauf 17:50 Werktage). |
| Oberflächen (Watchlist/RSS-Quellen/Lernvorschlag-Freigabe) | 🟢 | A1/A3/A4/A5/A6/A7/A9/A11 weiterhin live gegen die echten Webhooks verifiziert. **A8 (hoch, zentrale Wertebereichs-Regeltabelle für alle 5 Lernvorschlags-Aktivierungspfade) jetzt ebenfalls behoben**, löst A7 vollständig ab. Nur noch A2 offen (siehe unten) - bewusst zurückgestellter Architekturumbau, keine akute Lücke. Höchster Vertrauensgrad aller Bereiche. |
| Dokumentation | 🟡 | `FEHLERANALYSE.md`/`AENDERUNGSPROTOKOLL.md`/`TESTPLAN.md`/`PRODUKTIONSFREIGABE.md`/`OFFENE_AUFGABEN.md` durchgängig aktuell gehalten. G4 (Live-IDs für `09b`/`12`/`13`/`14` nicht im Repo verifizierbar) weiterhin offen. |

## A2 (hoch, bewusst zurückgestellt)

„Feste Stored Procedures pro Aktion mit typisierten Parametern statt String-Interpolation" für `Watchlist verwalten`/`RSS-Quellen verwalten`/`12` - nach Rücksprache mit dem Nutzer am 2026-08-02 **nicht** in dieser Runde umgesetzt, da ein voller Architekturumbau (neue SQL-Funktionen + Neuverdrahtung aller Postgres-Nodes in 3 bereits mehrfach überarbeiteten, produktiv laufenden Webhook-Workflows + kompletter Retest) ein unverhältnismäßiges Regressionsrisiko in derselben Sitzung bedeutet hätte. Die akute Injection-/Validierungslücke selbst ist bereits vollständig geschlossen (A1/A3/A4/A9/A11 kritisch, A8 hoch) - A2 bleibt eine strukturelle Verteidigungstiefe-Verbesserung für künftige Änderungen, kein aktuell ausnutzbares Loch. Siehe `OFFENE_AUFGABEN.md` für den Umsetzungsvorschlag einer künftigen Sitzung.

## Zusammenfassende Empfehlung

**Keine Freigabe für den produktiven, automatisierten Betrieb von Workflow `14` (Portfolio-Risiko/Paper-Trading) oder `09b` (Lernagent Handelsstrategien), bevor mindestens ein manueller Testlauf von `14` gegen echte Daten beobachtet wurde** - die Migration ist jetzt ausgeführt, die fachliche Logik vollständig gehärtet, es fehlt nur noch die Live-Beobachtung.

**Die Oberflächen (Watchlist, RSS-Quellen, Lernvorschlag-Freigabe) und die Empfehlungslogik (06/12) sind jetzt durchgängig live/lokal verifiziert und können mit hohem Vertrauen als abgesichert gelten** - sowohl der ursprüngliche Kern des Auftrags (SQL-Injection/SSRF/Formular-Manipulation) als auch das Wertebereichs-Regelwerk für Lernvorschläge sind vollständig geschlossen.

**Die News-Pipeline (03/08) läuft weiterhin aktiv** (stündlich/geplant, nicht unterbrochen) und hat mit dieser Runde eine spürbar präzisere Deduplizierung und Baseline-Bestimmung erhalten, ohne dass ein isolierter Testlauf vor dem nächsten planmäßigen Durchlauf möglich war.

**Nächster Schritt vor jeder weiteren Freigabe-Entscheidung:** `sql/040` ausführen (rein kosmetisch, kein Risiko); einen echten Lauf von `00` (Orchestrator, 17:50 Werktage) und `14` (manuell) beobachten; danach die verbleibenden 21 "mittel"/6 "niedrig" eingestuften Funde nach Bedarf angehen.
